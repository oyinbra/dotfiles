# Ignore the 'node_modules' directory, which contains third-party libraries and dependencies.
node_modules

# Ignore system and editor files
.DS_Store
Thumbs.db
.idea/

# Ignore Python virtual environment
venv/

# Ignore 'package-lock.json', a file generated by npm to lock dependencies versions.
package-lock.json

# Ignore the '.vscode' directory, which contains Visual Studio Code settings.
.vscode/

# Ignore '.idea' directory, which contains settings for JetBrains IDEs like PyCharm.
*.idea

# Ignore '.vscode' directory, another potential location for Visual Studio Code settings.
*.vscode

# Ignore the '.git' directory, which contains Git repository metadata.
*.git

# Ignore all workspace files
*.code-workspace

# Ignore '.env' files, typically used for environment configuration.
*.env

# Ignore Python compiled files.
*.pyc

# Ignore the '.git' directory in any subdirectories.
**/.git

# To ignore an entire folder, create a separate .gitignore file in that folder with just '*' in it.
# This rule is a comment explaining how to ignore a folder using its own .gitignore file.

