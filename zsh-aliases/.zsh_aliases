# ##############################################
# ################### APT ######################
# ##############################################

# UPDATE and INSTALL PACKAGES
alias apt-uu="sudo apt update && sudo apt upgrade"
alias apt-ud="sudo apt update"
alias apt-ug="sudo apt upgrade"
alias apt-get="sudo apt-get update && apt-get upgrade"
# Delete apt-repo
alias apt-d="sudo add-apt-repository --remove"
# Add apt-repo
alias apt-a="sudo add-apt-repository"
# List Upgradable packages
alias apt-l='apt list --upgradable'
# Clean Packages
alias apt-c='sudo apt autoclean && sudo apt autoremove'

# ##############################################
# ################### NALA #####################
# ##############################################

# UPDATE and INSTALL PACKAGES
alias nala-uu="sudo nala update && sudo nala upgrade"
alias nala-ud="sudo nala update"
alias nala-ug="sudo nala upgrade"
alias nala-i="sudo nala install"
alias nala-r="sudo nala remove"

# ##############################################
# ################### PACMAN ###################
# ##############################################

# UPDATE and INSTALL PACKAGES
alias pacman-ud="sudo pacman -Sy"
alias pacman-ug="sudo pacman -Syu"
alias pacman-uu="sudo pacman -Syyu"
alias pacman-i="sudo pacman -Syu"
alias pacman-r="sudo pacman -R"

# ##############################################
# ################### YAY ######################
# ##############################################

# UPDATE and INSTALL PACKAGES
alias yay-ud="yay -Sy"
alias yay-ug="yay -Syu"
alias yay-uu="yay -Syyu"
alias yay-i="yay -Syu"
alias yay-r="yay -R"

# ##############################################
# ################### PARU ######################
# ##############################################

# UPDATE and INSTALL PACKAGES
alias paru-ud="paru -Sy"
alias paru-ug="paru -Syu"
alias paru-uu="paru -Syyu"
alias paru-i="paru -Syu"
alias paru-r="paru -R"

# ##############################################
# ############### FLATPAK ######################
# ##############################################

alias flatpak-i="flatpak install"
alias flatpak-r"flatpak remove"

# ##############################################
# ############### SNAP #########################
# ##############################################

alias snap-i="sudo snap install"
alias snap-r="sudo snap remove"

# ##############################################
# ################# GIT ########################
# ##############################################

alias gi="git init"
alias cl="clear"
alias n="nano"
alias v="code"
alias m="mkdir"
alias g="git"
alias ga="git add"
alias gaa="git add --all"
alias grao="git remote add origin"
# Git commit message
alias gcmsg="git commit -m"
# Git status
alias gst="git status"
# Git Push
alias gp="git push"

# ##############################################
# ############## DIRECTORY ALIASES #############
# ##############################################

# Refresh zsh
alias rf="source ~/.zshrc"

# Zsh config
alias zshconfig="code ~/.zshrc"

# Bash config
alias bashconfig="code ~/.bashrc"

# Neofetch Distro
alias nfconfig="code ~/.config/neofetch/config.conf"

# Zsh aliases config
alias aliasconfig="code ~/.zsh_aliases"

# Dotfiles
alias dotfiles="~/.dotfiles"

# Work Directory
alias w="cd ~/Workspace"

# Project Directory
alias p="cd ~/Workspace/projects"

# Repository Packages
alias repos="~/Repos"

# Pictures Folder
alias pictures="~/Pictures"

# Coinmarketcapital
alias cmc='py ~/Workspace/Python/cmc.py'

# ##############################################
# ############## ACTION ALIASES ################
# ##############################################

# Check Git current version
alias gv="git --version"

# Watch SASS and output to dist folder
alias wsod="sass --watch scss:dist/css"

# List Color
alias ls="ls --color=auto"
# List All
alias la="ls -all"

# Python
alias py="python3"
alias python="python3"

# Python version
alias pv="py --version"

# Pip3 List
alias pl="pip3 list"

# Switch Python Version
alias pyconfig="sudo update-alternatives --config python3"

# Terminal config
alias tconfig="sudo update-alternatives --config x-terminal-emulator"

# Delete locked Directory or files
alias rmv="rm -rf"

# List Users
alias lusers="cat /etc/passwd"

# Hibernate
alias hbn='sudo systemctl hibernate'

# Nethogs
alias nethogs="sudo nethogs"

# Nefetch
alias nf="neofetch"
